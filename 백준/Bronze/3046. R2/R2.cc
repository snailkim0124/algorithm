#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int r1, s;
int r2;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	cin >> r1 >> s;
	r2 = s * 2 - r1;
	cout << r2;
	return 0;
}