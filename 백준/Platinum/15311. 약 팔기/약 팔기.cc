#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int n;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	cin >> n;
	cout << 2000 << "\n";
	for (int i = 1; i <= 1000; i++) {
		cout << 1 << " ";
	}
	for (int i = 1; i <= 1000; i++) {
		cout << 1000 << " ";
	}
	
	return 0;
}